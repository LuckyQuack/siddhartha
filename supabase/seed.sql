-- ============================================================
-- Seed data for local development
-- ============================================================

-- Add development seed data here.
-- This file is run after migrations when using `supabase db reset`.

-- Example structure (fill in as needed):
-- INSERT INTO auth.users (id, email) VALUES (...);
-- INSERT INTO books (id, user_id, title, author, file_type) VALUES (...);
-- INSERT INTO highlights (id, user_id, book_id, selected_text, ...) VALUES (...);
